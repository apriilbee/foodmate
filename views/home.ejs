<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') %>
    <body>
        <%- include('partials/navbar') %>

        <!-- Welcome Section -->
        <div class="container" style="margin-top: 40px">
            <div class="welcome-message center-align" style="margin: 40px 0 20px">
                <h4 style="color: #fe8641; font-weight: 600; font-family: 'Bebas Neue', cursive">
                    Welcome back, <%= user.username %> 👋
                </h4>
                <p style="font-size: 18px; color: #666">Let's plan your next delicious meal!</p>
            </div>

            <div class="search-bar-container" style="margin: 30px 0">
                <div class="row">
                    <div class="input-field col s12 m8 offset-m2">
                        <div class="search-bar-wrapper" style="position: relative">
                            <i class="material-icons" style="position: absolute; top: 10px; left: 12px; color: #999">
                                search
                            </i>
                            <input
                                id="recipe-search-input"
                                type="text"
                                placeholder="What are you craving?"
                                class="white z-depth-1"
                                style="
                                    padding-left: 40px;
                                    border-radius: 9999px;
                                    height: 50px;
                                    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
                                "
                            />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Category Strip -->
            <%- include('partials/dietary-preference/category-strip') %>

            <!-- Diet Tags -->
            <%- include('partials/dietary-preference/diet-tags') %>

            <!-- Meal Card -->
            <%- include ('partials/recipes/recipe-options') %>

            <!-- AI Chat -->
            <%- include ('partials/aiModal') %>
        </div>
        <%- include('partials/scripts') %> <%- include("partials/recipes/add-to-meal-modal") %>
    </body>
</html>
